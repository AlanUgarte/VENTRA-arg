[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node_modules/.bin/prisma migrate deploy && node apps/api/dist/main"
healthcheckPath = "/api/billing/plans"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
